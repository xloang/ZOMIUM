RewriteEngine On
RewriteBase /

# Clean URLs are rendered by the Express app on port 3000.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ http://127.0.0.1:3000/$1 [P,L]
